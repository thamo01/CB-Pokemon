{
  "name": "PokemonBotTS",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "browserify": "browserify ./build/src/index.js -o ./build/src/browserified-index.js",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gts": "^0.6.0",
    "tinyify": "^2.4.2",
    "ts-loader": "^4.3.0",
    "typescript": "~2.8.0",
    "webpack-cli": "^2.1.4"
  }
}
